#include <stdio.h>

int main(void)

{

  int number[9];
  int sum = 0;
  int tmp = 0;


  while(tmp < 10){
    number[tmp] = tmp + 1;
    printf("%d",number[tmp]);
    sum = sum + number[tmp];
    tmp = tmp + 1;
  }

  printf("sum = %d",sum);

}
    
