#include <stdio.h>
#include <string.h>

int main(void)
{

  int chk;
  FILE *fp;
  char str[255];

  if((fopen("test.dat","W+")) == NULL){
    printf("FILE not found.Create new FILE.\n");
  }else{
    printf("FILE found.Oparate this FILE.\n");
  }

  return 0;

}
