#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int num[100];

int main(void)
{

  char barcode[100];
  char str[100];
  int sum = 0;
  

  static int i = 0;
  int t;
  scanf("%d",&t);
  
  for(i = 0;i < t;i++){
  printf("バーコードを読み込ませてください\n");
  scanf("%s",barcode);
  sprintf(barcode,"%d",num[i]);
  sum = num[i];

  }  

  return 0;

}
